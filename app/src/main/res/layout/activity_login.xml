<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_hp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_launcher"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="100dp"
        />

    <TextView
        android:id="@+id/tv_username"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignBottom="@id/iv_hp"
        android:layout_marginTop="100dp"
        android:layout_marginRight="0dp"
        android:layout_marginBottom="-80dp"
        android:layout_toLeftOf="@id/iv_hp"
        android:text="用户名："
        android:textSize="30sp" />

    <EditText
        android:id="@+id/et_username"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:hint="请输入用户名"
        android:layout_toRightOf="@id/tv_username"
        android:layout_alignBottom="@id/tv_username"
        android:background="@drawable/edbackground"
        android:gravity="center"
        />

    <TextView
        android:id="@+id/tv_passwords"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@id/tv_username"
        android:layout_alignRight="@id/tv_username"
        android:layout_marginTop="90dp"
        android:text="密码："
        android:textSize="30sp" />

    <EditText
        android:id="@+id/et_passwords"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_alignBottom="@id/tv_passwords"

        android:layout_toRightOf="@id/tv_passwords"
        android:background="@drawable/edbackground"
        android:gravity="center"
        android:hint="请输入密码"
        android:inputType="textPassword" />

    <Button
       android:layout_marginTop="50dp"
        android:id="@+id/btn_login"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tv_passwords"
        android:text="登录"
        android:textSize="20sp"
        android:background="@drawable/login_button_bg"/>
    <Button
        android:id="@+id/btn_reg"
        android:layout_marginTop="10dp"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/btn_login"
        android:layout_alignLeft="@id/btn_login"
        android:text="注册"
        android:textSize="20sp"
        android:background="@drawable/login_button_bg"/>

</RelativeLayout>